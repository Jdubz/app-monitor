version: '3.8'

services:
  bot-b:
    container_name: dev-bot-b
    image: claude-worker:latest
    build:
      context: ../../
      dockerfile: app-monitor/dev-bots/docker/Dockerfile

    # Mount the entire bot-b volume as the workspace
    volumes:
      - ./volumes/bot-b:/workspace:rw
      - bot-b-logs:/workspace/logs
      - bot-b-data:/workspace/.claude
      - bot-b-npm-cache:/home/worker/.npm-global
      - bot-b-python-cache:/home/worker/.cache

    working_dir: /workspace

    environment:
      - BOT_ID=bot-b
      - NODE_ENV=development
      - WORKSPACE_ROOT=/workspace
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1

    # Read env from the bot's volume
    env_file:
      - ./volumes/bot-b/.env

    # Keep container running
    command: /bin/bash -c "tail -f /dev/null"

    # Network configuration
    networks:
      - dev-bots-network

    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G

    # Health check
    healthcheck:
      test: ["CMD", "test", "-d", "/workspace/job-finder-BE"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

    # Restart policy
    restart: unless-stopped

volumes:
  bot-b-logs:
    driver: local
  bot-b-data:
    driver: local
  bot-b-npm-cache:
    driver: local
  bot-b-python-cache:
    driver: local

networks:
  dev-bots-network:
    name: dev-bots-network
    driver: bridge
