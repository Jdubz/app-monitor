# Production environment - connects to backend via nginx proxy
# Empty string means use the same host as the frontend (works from any machine)
# Nginx proxies /api/* to the active backend port (5001 or 5002)
VITE_API_BASE_URL=
