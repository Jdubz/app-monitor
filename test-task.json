{
  "type": "implementation",
  "title": "Add HMAC signature verification to GitHub webhook routes",
  "description": "Add HMAC signature verification function and apply it to GitHub webhook POST endpoints. Verify X-Hub-Signature-256 header using crypto.timingSafeEqual. Add GITHUB_WEBHOOK_SECRET to config.",
  "documentation": "READ backend/src/routes/github-webhooks.routes.ts for current webhook handling. READ backend/src/config.ts for environment variable pattern. Add verification inline to webhook routes.",
  "investigation": {
    "required": true,
    "steps": [
      "READ github-webhooks.routes.ts to understand current webhook handling",
      "GREP for existing signature verification or crypto usage",
      "READ config.ts for environment variable pattern",
      "VERIFY no duplicate signature verification exists"
    ],
    "mustFind": [
      "Current webhook route handlers",
      "Environment configuration pattern"
    ],
    "mustNotDuplicate": [
      "Crypto utilities (use Node.js built-in crypto module)",
      "Environment variable loading (use existing config pattern)"
    ]
  },
  "preImplementationChecklist": [
    "[ ] Read github-webhooks.routes.ts current implementation",
    "[ ] Check config.ts for environment variable pattern",
    "[ ] Verify no existing signature verification"
  ],
  "acceptanceCriteria": [
    "EXACTLY one function added: verifyGitHubSignature(payload, signature, secret)",
    "Function uses crypto.timingSafeEqual for timing-safe comparison",
    "GITHUB_WEBHOOK_SECRET added to backend/src/config.ts",
    "Verification added to /pr and /push webhook routes only",
    "Invalid signatures return 401 Unauthorized",
    "Valid signatures pass through unchanged",
    "MUST NOT create separate middleware file",
    "MUST NOT add external crypto libraries"
  ],
  "files": [
    "backend/src/routes/github-webhooks.routes.ts",
    "backend/src/config.ts"
  ],
  "modifyOnly": [
    "backend/src/routes/github-webhooks.routes.ts",
    "backend/src/config.ts"
  ],
  "doNotModify": [
    "backend/src/services/githubWebhookHandler.service.ts"
  ],
  "doNotCreate": [
    "backend/src/middleware/webhookAuth.ts (add inline to routes - reuse existing route patterns)",
    "backend/src/utils/crypto.ts (reuse Node.js crypto module directly - no wrapper needed)"
  ],
  "constraints": {
    "must": [
      "use Node.js built-in crypto module",
      "add function inline to routes file",
      "use timing-safe comparison"
    ],
    "mustNot": [
      "create separate middleware file",
      "change webhook handler signatures",
      "modify webhook payload structure"
    ],
    "exactDeliverables": {
      "functions": [
        "verifyGitHubSignature"
      ],
      "config_vars": [
        "GITHUB_WEBHOOK_SECRET"
      ]
    }
  },
  "gitWorkflow": {
    "required": true,
    "branch": "feat/webhook-hmac-verification",
    "commitMessage": "feat: add HMAC signature verification to webhooks\n\nVerifies X-Hub-Signature-256 header.\n\n\ud83e\udd16 Generated by dev-bot"
  },
  "estimatedEffort": {
    "hours": 1.5,
    "complexity": "simple",
    "confidence": "high"
  },
  "priority": 7,
  "metadata": {
    "promptEngineeringVersion": "v3",
    "strictScopeEnforcement": true,
    "mandatoryInvestigation": true,
    "duplicateProtection": true,
    "category": "security",
    "phase": "1-foundation"
  },
  "architectureReferences": [
    "backend/src/routes/github-webhooks.routes.ts - Current webhook route handlers",
    "backend/src/config.ts - Environment variable configuration pattern",
    "backend/src/middleware/auth.ts - Example of Express middleware pattern for authentication"
  ]
}